---
"@iterate-com/capnweb": minor
---

Iterate fork release: WebSocket-over-RPC (upgrade Responses), server-side `onCall` per-call hook for observability, and a Provider type tweak for better go-to-definition. Published as `@iterate-com/capnweb` so dependents can pin the fork via package alias without colliding with upstream `capnweb`.
